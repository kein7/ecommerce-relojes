import * as React from 'react'
import {
  AppBar,
  Box,
  Toolbar,
  Typography,
  Button,
  IconButton
} from '@mui/material'
import { Link, BrowserRouter, Route, Routes } from 'react-router-dom'

export default function UserPage() {

  return (<Box>
    <Typography variant="h2" sx={{mt:20}}>Mi perfil</Typography>
</Box>)
}