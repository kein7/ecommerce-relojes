import * as React from 'react'
import {
  AppBar,
  Box,
  Toolbar,
  Typography,
  Button,
  IconButton
} from '@mui/material'

export default function WishList() {
  return (
    <Box>
      <Typography variant="h2" sx={{ mt: 20 }}>
        Lista de deseos
      </Typography>
    </Box>
  )
}
